node_modules
.next
artifacts/browser-qa/
.env
*.tsbuildinfo
dev.log
next-env.d.ts
.env.backup*

# Database backups — contain real emails and credential hashes
backups/

# Raw ClickUp export — 1.2MB of real task data including personal email
# addresses. Re-fetch it with the command in scripts/migrate-clickup.mjs rather
# than committing it.
scripts/clickup-export.json

# Database password for the Mumbai project — never commit
.db-password-mumbai.txt
backups/mumbai-migration/.env.sydney

# local secrets
.env
.env.local
.env*.local
/env
